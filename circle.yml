database:
  override:
    - bundle exec rake db:create db:schema:load --trace:
        environment:
          RAILS_ENV: test
          RACK_ENV: test
dependencies:
  override:
    - bundle exec install



