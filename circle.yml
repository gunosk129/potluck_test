database:
  override:
    - bundle exec rake db:create db:schema:load --trace:
        environment:
          RAILS_ENV: test
          RACK_ENV: test
dependencies:
  pre:
    - gem install mime-types-data -v '3.2016.0521'
  override:
    - bundle install



